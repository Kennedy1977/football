# v0.0.57 - Introduce patch notes and git workflow format

Date: 2026-03-11
Type: docs
Scope: workflow

## Summary

- Standardized release notes and git workflow so every shipped update has traceable patch notes and consistent commit/push metadata.

## Added

- New changelog file: `CHANGELOG.md`.
- Patch notes workflow docs and template:
  - `docs/patch-notes/README.md`
  - `docs/patch-notes/_template.md`
- Commit + push format guide:
  - `docs/git-commit-and-push-format.md`
- Git commit template skeleton:
  - `.gitmessage.txt`
- Generator command:
  - `npm run notes:new`
  - `scripts/notes-new.mjs`

## Changed

- `package.json` now includes `notes:new` script.
- `README.md` now references changelog, patch notes, and commit/push format workflow.

## Fixed

- None.

## Removed

- None.

## Validation

- [x] npm run typecheck
- [x] Manual QA completed for docs/scripts outputs and generated patch note file creation.

## Risks / Follow-up

- `notes:new` currently creates files only; it does not auto-update `CHANGELOG.md`.
- If desired, next step can auto-link each generated note into `CHANGELOG.md` under `Unreleased`.
