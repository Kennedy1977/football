# v0.0.59 - Fix footer version typing and confirm dependency currency

Date: 2026-03-11
Type: fix
Scope: build-footer

## Summary

- Fixed footer version state typing to avoid narrow literal setter issues in stricter TypeScript inference paths and confirmed that `next` and `typescript` are already on current compatible versions for this codebase.

## Added

- None.

## Changed

- `apps/web/src/components/build-footer.tsx` now uses `useState<string>(APP_VERSION_LABEL)` to guarantee a stable string setter type.

## Fixed

- Prevented potential deployment type-check failure where dynamic runtime version strings could conflict with a literal-inferred state type.

## Removed

- None.

## Validation

- [x] npm run typecheck
- [x] npm outdated next typescript (no outdated results)

## Risks / Follow-up

- Local `npm run build` still requires Node `>=20.9.0`; this environment is Node `18.19.0`, so full Next production build cannot be validated locally here.
