# v0.0.82 - update pre commit

Date: 2026-03-12
Type: build
Scope: protocol

## Summary

- Automates commit protocol prep so required version/build/docs artifacts are generated and staged before validation.
- Reduces commit failures by auto-filling `Patch-Notes` from staged patch note files.

## Added

- New script: `scripts/protocol-auto-stage.mjs`.
- New npm command: `npm run protocol:auto`.

## Changed

- `.githooks/pre-commit` now runs protocol auto-stage before staged-file validation.
- Commit guide now documents auto protocol behavior and manual fallback command.

## Fixed

- `scripts/protocol-check-commit-msg.mjs` now auto-fills `Patch-Notes` when template placeholder/blank line is left in commit message.
- `scripts/protocol-auto-stage.mjs` now stages the patch note it generates, avoiding protocol failures.

## Removed

- None.

## Validation

- [x] npm run typecheck
- [ ] Manual QA completed on affected screens/routes.

## Risks / Follow-up

- Auto-generated patch note titles are generic when no `PROTOCOL_NOTES_TITLE` override is provided.
